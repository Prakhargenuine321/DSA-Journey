#include<bits/stdc++.h>
using namespace std;

//Unordered Set -->lower_bound and upper_bound functions doesn't work.
//Same as "SETS" only difference is that it doesn't have "SORTED".
//It has O(1) time complexity for all operations except once in a million case where it went to O(N).

int main(){


    //declaration
    unordered_set<int> unorderSet;
    
    return 0;
}